# Music-Aware Virtual Assistants — project folder

Self-contained. `index.html` is the demo page (served at /musicassistant/).
Drop more material here and reference it relatively from index.html:
- `musicassistant.mp4` — a ~30s preview video (falls back to YouTube until added)
- extra figures, audio, etc.
