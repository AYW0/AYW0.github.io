# The site is now hand-authored static HTML (no theme, no collections).
# Jekyll simply copies the files through; the legacy academicpages theme lives
# in _archive/, which Jekyll ignores automatically (leading underscore) so it
# is never deployed. You can delete _archive/ once you're sure nothing there
# is needed.
title: "Alexander Wang"
url: "https://ayw0.github.io"
